<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/static/css/style.css">
</head>

<body>

    <div id="container">

        {{#if errors}}
        <div>
            <div class="notification errorBox">
                {{#each errors}}
                <div>{{msg}}</div>
                {{/each}}
            </div>
        </div>
        {{/if}}

        <nav>
            <div class="left-container">
                <ul>
                    <li><a href="/">Home</a></li>
                    {{#if hasUser}}
                    <li><a href="/create">Add +</a></li>
                    {{else}}
                    <li><a href="/login">Login</a></li>
                    <li><a href="/register">Register</a></li>
                    {{/if}}
                </ul>
            </div>
            {{#if hasUser}}
            <div class="right-container">
                <a href="/profile" class="log-out">{{user.username}}</a>
                <a href="/logout" class="log-out">Logout</a>
            </div>
            {{/if}}
        </nav>

        {{{body}}}

        <footer>&copy;SoftUni - JS Back-end - BookingUni 2020</footer>
    </div>

</body>

</html>